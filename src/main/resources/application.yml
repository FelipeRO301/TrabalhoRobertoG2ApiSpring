server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/db_album
    username: postgres
    password: f14tomcat20e20
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgresPlusDialect
    show-sql: true
